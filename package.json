{
  "name": "mantra",
  "displayName": "Mantra",
  "publisher": "mishra7yash",
  "icon": "mantralogo.png",
  "version": "0.2.9",
  "description": "Control VS Code with your voice. Extremely accurate, absurdly fast.",
  "engines": {
    "vscode": "^1.98.0"
  },
  "categories": [
    "Other",
    "Machine Learning"
  ],
  "main": "./out/src/extension.js",
  "extensionKind": [
    "ui",
    "workspace"
  ],
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": false,
      "description": "Records audio and may read file contents to send to cloud services; disabled in Restricted Mode."
    },
    "virtualWorkspaces": {
      "supported": false,
      "description": "Not supported: needs access to local audio devices."
    }
  },
  "contributes": {
    "viewsContainers": {
      "activitybar": [
        {
          "id": "mantra",
          "title": "Mantra",
          "icon": "media/mantra-icon.svg"
        }
      ]
    },
    "views": {
      "mantra": [
        {
          "type": "webview",
          "id": "mantra.sidebar",
          "name": "Mantra"
        }
      ]
    },
    "commands": [
      {
        "command": "mantra.start",
        "title": "Mantra: Start Recording"
      },
      {
        "command": "mantra.pause",
        "title": "Mantra: Stop Listening"
      },
      {
        "command": "mantra.stopAndTranscribe",
        "title": "Mantra: Stop & Transcribe"
      },
      {
        "command": "mantra.pttStart",
        "title": "Mantra: Push to Talk (Start)"
      },
      {
        "command": "mantra.pttStop",
        "title": "Mantra: Push to Talk (Stop)"
      },
      {
        "command": "mantra.configurePrompt",
        "title": "Mantra: Configure Prompt"
      },
      {
        "command": "mantra.toggleCommandsOnly",
        "title": "Mantra: Toggle Commands-Only Mode"
      },
      {
        "command": "mantra.openSettings",
        "title": "Mantra: Open Settings"
      },
      {
        "command": "mantra.editCerebrasApiKey",
        "title": "Mantra: Set Cerebras API Key"
      },
      {
        "command": "mantra.editGroqApiKey",
        "title": "Mantra: Set Groq API Key"
      },
      {
        "command": "mantra.editDeepgramApiKey",
        "title": "Mantra: Set Deepgram API Key"
      },
      {
        "command": "mantra.editAquavoiceApiKey",
        "title": "Mantra: Set Aqua Voice API Key"
      },
      {
        "command": "mantra.editAssemblyaiApiKey",
        "title": "Mantra: Set AssemblyAI API Key"
      },
      {
        "command": "mantra.selectMicrophone",
        "title": "Mantra: Select Microphone"
      },
      {
        "command": "mantra.focusClaude",
        "title": "Mantra: Focus Claude Code Panel"
      },
      {
        "command": "mantra.testMicrophone",
        "title": "Mantra: Test Microphone"
      },
      {
        "command": "mantra.focusAgent",
        "title": "Mantra: Focus Agent Panel"
      },
      {
        "command": "mantra.toggleSendContext",
        "title": "Mantra: Toggle Send Context to Agent"
      }
    ],
    "keybindings": [
      {
        "command": "mantra.start",
        "key": "ctrl+shift+1"
      },
      {
        "command": "mantra.pause",
        "key": "ctrl+shift+2"
      },
      {
        "command": "mantra.stopAndTranscribe",
        "key": "ctrl+shift+3"
      },
      {
        "command": "mantra.openSettings",
        "key": "ctrl+shift+4"
      },
      {
        "command": "mantra.selectMicrophone",
        "key": "ctrl+shift+5"
      }
    ],
    "configuration": {
      "title": "Mantra",
      "properties": {
        "mantra.agentBackend": {
          "type": "string",
          "enum": [
            "claude",
            "none"
          ],
          "default": "none",
          "description": "Which AI agent to use for voice-directed prompts. 'none' means unclassified speech goes to Quick Question mode instead of an agent."
        },
        "mantra.prompt": {
          "type": "string",
          "default": "You are a precise router and code editor. You are trying to classify the instruction and generate the appropriate payload.\nYou will receive the file name (if known), a general editor context, a brief cursor summary, an \"enclosing symbol\" section (the smallest function/class/etc. containing the cursor), and the ENTIRE file contents.\nKeep in mind that the instructions you receive will be based off of a speech-to-text transcript, so use your best judgment to understand what the user was trying to say as there may be inaccuracies in the transcription.\n\nCRITICAL: Make ONLY the exact change the user asked for. Do NOT fix, clean up, or \"improve\" anything else in the file — no indentation fixes, no whitespace cleanup, no trailing newline changes, no reformatting of unrelated code. Every line you did not intentionally change must be byte-for-byte identical to the original.\n\nOutput format MUST be:\nquestion <answer to give the user>\ncommand <one canonical command phrase from the provided list>\nmodification <updated content \u2014 no code fences>\nterminal <the exact shell command to type in the terminal>\nagent <prompt to forward to the AI agent>\n\nRules:\n1) The VERY FIRST WORD of your response must be exactly one of: question, command, modification, terminal, agent. Nothing else before it — no \"label:\", no quotes, no markdown, no reasoning, no preamble. The first characters you output must be one of these five words.\n2) Then a single space, then the payload.\n3) For command: ONLY for IDE/editor operations \u2014 the user must be asking for an editor action, NOT asking you to write or generate code. Pick the closest canonical PHRASE from the provided list. Do NOT invent new ones.\n   CRITICAL: \"make\", \"create\", \"build\", \"write\", \"generate\", or \"set up\" followed by a CODE CONCEPT (pipeline, API, function, class, component, app, server, script, etc.) is NEVER a command \u2014 it is a modification or agent request. The word \"create\" only maps to the command \"create file\" if the user literally wants an empty new file/tab, NOT if they want code to be generated.\n   Commands would be like:\n   - \"please cut the first three lines\" -> command cut lines 1 to 3\n   - \"can you undo that?\" -> command undo\n   - \"line four\" -> command go to line 4\n   - \"select lines twenty to twenty-five\" -> command select lines 20 to 25\n   - \"save file\" -> command save file\n   - \"close tab\" -> command close tab\n   NOT commands (these are modifications or agent):\n   - \"make an ETL pipeline\" -> agent (complex feature)\n   - \"create a React component\" -> agent (complex feature)\n   - \"build a REST API\" -> agent (complex feature)\n   - \"write a sorting function\" -> agent (complex feature)\n4) For modification:\n   - Use this ONLY for small, targeted, single-file edits that you can do directly and confidently. Examples of modification-appropriate tasks:\n     - \"change this for loop to a while loop\" -> modification\n     - \"rename this variable to count\" -> modification\n     - \"remove the comments from this function\" -> modification\n     - \"add a print statement here\" -> modification\n     - \"make the board size bigger\" -> modification\n     - \"change the color to red\" -> modification\n     - \"total underscore sum equals zero\" (dictating code) -> modification\n     - \"for i in range len nums print i\" (dictating code) -> modification\n     - \"opponent underscore icon underscore size equals 10\" (dictating code) -> modification\n   - CODE DICTATION: When the user is dictating code to insert (variable assignments, loops, expressions, function calls), interpret underscores, operators, and punctuation literally \u2014 \"x underscore y equals 5\" means `x_y = 5`, \"for i in range len nums\" means `for i in range(len(nums)):`. INSERT the dictated code at the cursor position or the most logical nearby location. In SELECTION MODE, include ALL the original selected lines plus the new code inserted at the right spot. In NORMAL MODE, output the ENTIRE file with the new code inserted \u2014 NEVER output just the dictated line(s) alone, that would erase the entire file.\n   - For ANYTHING more complex \u2014 new features, refactoring, multi-step changes, architectural changes, adding AI, adding multiplayer, etc. \u2014 use \"agent\" instead. Examples:\n     - \"add an AI opponent\" -> agent (not modification)\n     - \"make it multiplayer\" -> agent (not modification)\n     - \"add authentication\" -> agent (not modification)\n     - \"refactor this to use async/await\" -> agent (not modification)\n     - \"add error handling throughout\" -> agent (not modification)\n     - \"use NumPy throughout the code\" -> agent (not modification)\n     - \"improve the performance\" -> agent (not modification)\n   - SELECTION MODE: If the user message contains \"SELECTION MODE\", output ONLY the replacement for the selected text. Do NOT output the entire file. Put the label 'modification' on its own line, then start the replacement code on the NEXT line with the EXACT same indentation as the original selected text. CRITICAL: Include ALL the original lines from the selection with only the requested changes applied. For code dictation/insertion, reproduce all the original selected lines and INSERT the new code at the cursor position. Never drop or omit existing lines \u2014 your output replaces the selection entirely, so any line you omit will be deleted. Do NOT put code on the same line as the label.\n   - NORMAL MODE (no selection): produce the full file contents with ALL requested changes applied.\n   IMPORTANT: In NORMAL MODE, double-check that you are outputting the COMPLETE file with all original lines preserved (plus your changes). If your output is significantly shorter than the input, make sure the user actually asked to remove that much content — accidentally dropping lines destroys the user's code.\n   - Use the ENTIRE file contents provided as the source of truth. You may modify ANY part of the file as needed by the instruction.\n   - Use the cursor summary and enclosing symbol ONLY to interpret relative phrases like \"this line\" or \"this function\"; do not restrict edits to that region.\n   - Return ONLY the final file content as the payload \u2014 no markdown fences, no language tags, no prose, no extra quotes or backticks at the beginning or end.\n   - ONLY change what the user asked for. Do NOT touch any other lines — no indentation fixes, no whitespace changes, no trailing newline removal, no style cleanups. If a line was not part of the user's request, it must appear exactly as it was in the original.\n5) For question: ONLY use this in two cases:\n   a) The user explicitly says \"quick question\" \u2014 e.g. \"quick question, what does this line do?\" -> question ...\n   b) No agent is available (you will be told whether an agent is active) AND the request is a knowledge/explanation question \u2014 e.g. \"what does this line do?\", \"explain this function\", \"what's the time complexity?\".\n   If an agent IS available, NEVER output \"question\". Route knowledge questions to \"agent\" instead \u2014 the agent can answer them too. Your answer should be concise.\n6) For terminal: use this when the user wants to run a shell command, script, or CLI tool. Translate their natural language into the exact shell command. Consider the active file name, language, and workspace context. IMPORTANT: Always use `python3` (not `python`) for Python commands.\n   Terminal commands are EXECUTED by default. If the user explicitly asks to wait or not run it (e.g. \"but don't run it\", \"and wait\", \"just type it\", \"hold on\"), the command will only be typed without executing \u2014 but you do NOT need to handle this; just output the shell command as usual.\n   Examples:\n   - \"run this file\" -> terminal python3 main.py (or node index.js, etc., based on file language)\n   - \"create a virtual environment\" -> terminal python3 -m venv venv\n   - \"install the requests library\" -> terminal pip3 install requests\n   - \"check git status\" -> terminal git status\n   - \"run that in a tmux session\" -> terminal tmux new-session -d 'python3 main.py'\n   CRITICAL TERMINAL RULES: \"run X\", \"execute X\", \"npm X\", \"git X\", \"pip X\", \"docker X\", \"brew X\", \"apt X\", \"cargo X\", \"yarn X\" → ALWAYS terminal, NEVER command. These are shell/CLI operations, not editor operations.\n   Keywords that signal terminal: run, execute, install, build, test, check, deploy, start, stop, push, pull, commit, clone, init, serve, compile, lint, format, migrate, seed, publish.\n7) For agent: use this for ANY non-trivial task. This is the DEFAULT when an agent is available and the request doesn't clearly fit command/terminal/modification. The agent can handle complex coding, multi-step work, new features, debugging, refactoring, and open-ended requests. Route here even if the user doesn't explicitly mention the agent.\n   IMPORTANT: Output the user's words EXACTLY as they said them. Do NOT rephrase, expand, add detail, or inject context. The agent receives context separately. Just pass through the user's raw request. Only fix obvious speech-to-text errors.\n   Examples of what goes to agent:\n   - \"add an AI to the game\" -> agent add an AI to the game\n   - \"how can this be improved?\" -> agent how can this be improved?\n   - \"make it faster\" -> agent make it faster\n   - \"add tests\" -> agent add tests\n   - \"add dark mode\" -> agent add dark mode\n8) If the instruction is ambiguous and an agent is available, prefer \"agent\" \u2014 let the agent figure it out.\n9) Never preface with explanations. No JSON. No labels other than the five.\n10) Do NOT change the user's formatting, spacing, indentation, or whitespace unless they explicitly ask you to. Do NOT remove or add blank lines, fix alignment, or clean up code that the user did not mention. Unrelated lines must be copied verbatim.\n11) Do NOT think, reason, or deliberate — give the answer IMMEDIATELY. No <think> blocks, no preamble, no \"Let me think about this\", no \"Okay, let's see\". The very first characters of your response must be one of the five labels. Output ONLY the label and payload.\n12) FOLLOW-UP / CORRECTION: Watch for correction words like \"actually\", \"instead\", \"no wait\", \"reverse that\", \"do the opposite\". These often refer to the most recent action in the Activity Log. When you see them, check the log for context — e.g. if the log shows 'sorted imports alphabetically' and the user says 'actually, do reverse', they likely mean reverse-alphabetical order. Use the activity log to disambiguate whenever a short utterance could be a follow-up to a previous action.\n13) RELATIVE CODE NAVIGATION: When the user wants to navigate to a nearby code construct (\"go to the next else if\", \"jump to the catch block\", \"go to the next function\"), look at the file contents with line numbers and find the target. Output: command go to line N. The editor already handles \"go to line N\" natively. Examples:\n   - \"go to the next else if\" -> command go to line 47\n   - \"go to the next function\" -> command go to line 82\n   - \"jump to the catch block\" -> command go to line 55\n   - \"go to the return statement\" -> command go to line 33\n   Use the cursor position to determine \"next\" / \"previous\" relative to where the user currently is.",
          "markdownDescription": "System prompt used by Mantra when routing/editing. Reset to restore the built-in default.",
          "editPresentation": "multilineText"
        },
        "mantra.memoryPrompt": {
          "type": "string",
          "default": "You are a memory manager for a voice-controlled code editor session.\nYou maintain a thorough running summary of everything that has happened so far.\nGiven the current memory and the latest interaction, produce an UPDATED memory.\n\nYour memory should be written so that a future LLM reading it has full context to help the user. Include:\n- What project/files the user is working on and their current goals\n- Key decisions, code changes, and modifications made (with filenames and what changed)\n- Terminal commands run and their outcomes (successes, errors, what was installed/configured)\n- Questions the user asked and the answers they received \u2014 especially if the answer informs future work\n- User preferences, corrections, or patterns (e.g. \"user prefers python3\", \"user uses Claude for refactoring\")\n- Current state: what the user was last doing, any unresolved issues or next steps\n\nRules:\n- Be thorough but not verbose. Prioritize information that would be useful for the NEXT interaction.\n- Merge new info into existing context rather than appending a changelog.\n- Drop details that are fully superseded (e.g. old errors that were fixed).\n- Max ~500 words.\n- Output ONLY the updated memory text. No labels, no explanation.",
          "markdownDescription": "System prompt used by the memory manager to summarize session context. Reset to restore the built-in default.",
          "editPresentation": "multilineText"
        },
        "mantra.selectionPrompt": {
          "type": "string",
          "default": "You are a code selection assistant for a voice-controlled editor.\nThe user wants to select/highlight code. Given their voice command and a code file with line numbers, determine the exact lines to select.\n\nOutput EXACTLY one line in this format (nothing else):\nselect START END\n\nSTART and END are 1-based inclusive line numbers.\n\n=== RULES ===\n\n1. COMPLETE CONSTRUCTS: Always include the ENTIRE construct from header to closing line.\n- Functions: include `def`/`function`/method signature + all decorators above it\n- Classes: include `class` line through closing\n- if/elif/else: from first `if` through final `else`\n- try/except/finally: from `try` through final block\n- Loops: from loop header through body end\n\n2. NESTED CONSTRUCTS: When the user says \"inner\", \"inside\", \"nested\", select ONLY the inner construct, NOT the outer. When they say \"outer\" or \"whole\", include everything.\n\n3. ORDINAL SELECTION: \"the second elif\", \"the fourth elif\" — count ONLY elif branches, skip the initial if.\n   if → (skip) | elif A → 1st | elif B → 2nd | elif C → 3rd | elif D → 4th | else → \"the else\"\n   Select ONLY that single branch + body. NOT the whole chain.\n\n4. MULTI-CONSTRUCT: \"this function and the next two\" — select the current function plus the next N functions in sequence. Include blank lines between them.\n\n5. CONTENT-BASED: \"the log statements\", \"the imports\", \"the except blocks\" — select ALL matching lines/blocks. If they span a contiguous range, select that range. If scattered, select the range that covers all of them.\n\n6. RELATIVE RANGES:\n- \"the next N lines\" (cursor on line X) → select X+1 to X+N\n- \"from here to the end of the function\" → cursor line to function's last line\n- \"just this line\" / \"this line only\" → select cursor line to cursor line\n\n7. RESOLVING \"this\"/\"the\"/\"here\": Use the cursor position — refers to the NEAREST enclosing construct of that type.\n\n=== EXAMPLES ===\n\nOrdinal branches in an if/elif/else chain:\n```\n11: if temp < -40:\n12:     category = \"extreme_cold\"\n13:     alert = True\n14:     message = \"Dangerously cold\"\n15: elif temp < 0:\n16:     category = \"freezing\"\n17:     alert = False\n18:     message = \"Below freezing\"\n19: elif temp < 15:\n20:     category = \"cold\"\n21:     alert = False\n22:     message = \"Cold weather\"\n23: elif temp < 25:\n24:     category = \"comfortable\"\n25:     alert = False\n26:     message = \"Comfortable\"\n27: elif temp < 35:\n28:     category = \"warm\"\n29:     alert = False\n30:     message = \"Warm weather\"\n31: elif temp < 45:\n32:     category = \"hot\"\n33:     alert = True\n34:     message = \"Very hot\"\n35: else:\n36:     category = \"extreme_heat\"\n37:     alert = True\n38:     message = \"Dangerously hot\"\n```\n\"select the second elif\" → select 19 22 (elif temp < 15)\n\"select the fourth elif\" → select 27 30 (elif temp < 35)\n\"select the else block\" → select 35 38\n\"select the whole if elif else chain\" → select 11 38\nCounting elifs (skip the opening `if`, count only `elif` keywords):\nLine 11: if temp < -40 → NOT an elif (skip)\nLine 15: elif temp < 0 → 1st elif\nLine 19: elif temp < 15 → 2nd elif\nLine 23: elif temp < 25 → 3rd elif\nLine 27: elif temp < 35 → 4th elif ← \"select the fourth elif\" = select 27 30\nLine 31: elif temp < 45 → 5th elif\nLine 35: else → \"select the else\"\n\nNested loops:\n```\n4: for i, row in enumerate(matrix):\n5:     row_sum = 0\n6:     for j, val in enumerate(row):\n7:         if val > 0:\n8:             row_sum += val\n9:         else:\n10:            row_sum -= val\n11:    result.append(row_sum)\n```\n\"select the inner for loop\" (cursor 7) → select 6 10\n\"select the outer for loop\" (cursor 7) → select 4 11\n\"select this loop\" (cursor 7, inside inner) → select 6 10\n\"select the if statement\" (cursor 8) → select 7 10\n\nMulti-function:\n```\n7: def load_config(path):\n...\n13:     return data\n14:\n15: def validate_config(config):\n...\n26:     return config\n27:\n28: def read_input_files(input_dir):\n...\n43:     return files\n```\n\"select this function and the next two\" (cursor 10, inside load_config) → select 7 43\n\nContent-based:\n```\n9:     logger.info(f\"Loading config from {path}\")\n10:    with open(path) as f:\n11:        data = json.load(f)\n12:    logger.debug(f\"Config loaded: {len(data)} keys\")\n13:    return data\n```\n\"select the log statements in this function\" (cursor 10) → select 9 12 (covers both logger lines)\n\nElif inside a function with multiple elifs:\n```\n53: if action == \"uppercase\":\n54:     result[field] = str(result[field]).upper()\n55: elif action == \"strip\":\n56:     result[field] = str(result[field]).strip()\n57: elif action == \"default\":\n58:     if not result[field]:\n59:         result[field] = rule.get(\"value\", \"\")\n60: elif action == \"delete\":\n61:     del result[field]\n```\n\"select the second elif in this function\" → select 57 59 (elif action == 'default' + its body, which is the 2nd elif after 'strip')\nNote: \"strip\" is the 1st elif, \"default\" is the 2nd, \"delete\" is the 3rd.\nWait — actually count carefully: the 1st elif is \"strip\" (55-56), the 2nd is \"default\" (57-59), the 3rd is \"delete\" (60-61).\n\"select the second elif\" → select 57 59\n\nNested conditionals:\n```\n27: if item.get(\"type\") == \"A\":\n28:     if item.get(\"priority\") == \"high\":\n29:         output.append(\"URGENT\")\n30:     elif item.get(\"priority\") == \"medium\":\n31:         output.append(\"normal\")\n32:     else:\n33:         output.append(\"low\")\n34: elif item.get(\"type\") == \"B\":\n35:     for sub in item.get(\"children\", []):\n36:         if sub.get(\"active\"):\n37:             output.append(sub['name'])\n38: else:\n39:     output.append(\"unknown\")\n```\n\"select the inner if statement\" (cursor 29) → select 28 33\n\"select the outer if statement\" (cursor 29) → select 27 39\n\"select the for loop inside the elif\" (cursor 36) → select 35 37\n\nLine/range:\n\"select the next three lines\" (cursor on line 70) → select 71 73\n\"select from here to the end of the function\" (cursor 97, function ends 110) → select 97 110\n\"select just this line\" (cursor 25) → select 25 25\n\"select lines 10 to 20\" → select 10 20\n\"select the imports\" (imports at lines 1-3) → select 1 3\n\nTry/except:\n\"select the try except block\" (cursor inside try body) → select from `try` through final `except`\n\"select the except blocks\" → select just the except clauses and their bodies\n\nBy description:\n\"select the function that processes batches\" → select the entire process_batch function (def through return)\n\nFinal rules:\n- START and END are 1-based inclusive line numbers.\n- For ambiguous nesting, prefer the construct NEAREST to the cursor.\n- When in doubt, prefer slightly too wide over too narrow.\n- For ordinal selection (\"second elif\"), select ONLY that branch and its body.\n- Output ONLY the format above. No explanation, no reasoning, no extra text.",
          "markdownDescription": "System prompt for the selection model that determines edit scope. Reset to restore the built-in default.",
          "editPresentation": "multilineText"
        },
        "mantra.commandsOnly": {
          "type": "boolean",
          "default": false,
          "description": "Bypass the LLM completely. Only run canonical commands and text ops (no code edits, no answers)."
        },
        "mantra.llmProvider": {
          "type": "string",
          "enum": [
            "cerebras",
            "groq"
          ],
          "default": "groq",
          "description": "Which LLM provider to use for chat completions."
        },
        "mantra.llmModel": {
          "type": "string",
          "default": "",
          "description": "Which model to use for the selected LLM provider. Leave empty for the default model."
        },
        "mantra.groqApiKey": {
          "type": "string",
          "default": "",
          "description": "Groq API key. Leave empty to use the GROQ_API_KEY environment variable.",
          "scope": "application",
          "editPresentation": "password"
        },
        "mantra.cerebrasApiKey": {
          "type": "string",
          "default": "",
          "description": "Cerebras API key. Leave empty to use the CEREBRAS_API_KEY environment variable.",
          "scope": "application",
          "editPresentation": "password"
        },
        "mantra.sttProvider": {
          "type": "string",
          "enum": [
            "deepgram",
            "aquavoice",
            "assemblyai",
            "assemblyai-batch"
          ],
          "enumDescriptions": [
            "Deepgram Flux \u2014 streams live with turn detection",
            "Aqua Voice \u2014 transcribes after you finish speaking",
            "AssemblyAI Universal Streaming \u2014 streams live with turn detection",
            "AssemblyAI Universal 3 Pro \u2014 transcribes after you finish speaking"
          ],
          "default": "deepgram",
          "description": "Which speech-to-text provider to use."
        },
        "mantra.deepgramApiKey": {
          "type": "string",
          "default": "",
          "description": "Deepgram API key. Leave empty to use the DEEPGRAM_API_KEY environment variable.",
          "scope": "application",
          "editPresentation": "password"
        },
        "mantra.aquavoiceApiKey": {
          "type": "string",
          "default": "",
          "description": "Aqua Voice API key. Leave empty to use the AQUAVOICE_API_KEY environment variable.",
          "scope": "application",
          "editPresentation": "password"
        },
        "mantra.assemblyaiApiKey": {
          "type": "string",
          "default": "",
          "description": "AssemblyAI API key. Leave empty to use the ASSEMBLYAI_API_KEY environment variable.",
          "scope": "application",
          "editPresentation": "password"
        },
        "mantra.silenceTimeout": {
          "type": "string",
          "enum": [
            "0.5",
            "1",
            "1.5",
            "2",
            "3",
            "4",
            "5"
          ],
          "default": "2",
          "description": "Seconds of silence before Aqua Voice considers you done speaking. Higher = less sensitive."
        },
        "mantra.sensitivity": {
          "type": "string",
          "enum": [
            "low",
            "medium",
            "high"
          ],
          "default": "medium",
          "description": "Microphone sensitivity for silence detection. Low = requires louder speech (noisy environments), High = picks up quieter speech (quiet environments)."
        },
        "mantra.sendContext": {
          "type": "boolean",
          "default": true,
          "description": "Include activity log and terminal history as context when sending prompts to the AI agent."
        },
        "mantra.reasoningEffort": {
          "type": "string",
          "enum": [
            "low",
            "medium",
            "high"
          ],
          "default": "low",
          "description": "Set the reasoning effort level for the model (low, medium, high)."
        },
        "mantra.microphoneInput": {
          "type": "string",
          "default": "",
          "scope": "application",
          "markdownDescription": "Chosen microphone input **(set via Command Palette \u2192 Mantra: Select Microphone)**. Leave empty for automatic detection. Advanced users may paste raw FFmpeg input args here.\n- Windows: WASAPI/DirectShow\n- macOS: AVFoundation\n- Linux: PulseAudio"
        }
      }
    }
  },
  "license": "Proprietary",
  "scripts": {
    "compile": "tsc -p .",
    "watch": "tsc -w -p .",
    "pretest": "npm run compile && npm run lint",
    "test": "node ./out/test/runTest.js",
    "test:llm": "node test-llm.mjs",
    "lint": "eslint . --ext .ts",
    "package": "vsce package",
    "publish:marketplace": "vsce publish",
    "publish:openvsx": "ovsx publish --pat $OVSX_TOKEN",
    "vscode:prepublish": "npm run compile"
  },
  "dependencies": {
    "@deepgram/sdk": "^3.7.0",
    "ffmpeg-static": "^5.1.0",
    "@cerebras/cerebras_cloud_sdk": "^1.64.0"
  },
  "devDependencies": {
    "@types/node": "^20.12.12",
    "@types/vscode": "^1.98.0",
    "@vscode/test-electron": "^2.3.9",
    "eslint": "^9.6.0",
    "ovsx": "^0.9.0",
    "typescript": "^5.5.4",
    "@vscode/vsce": "^3.6.0"
  }
}